# Next.js Internationalization (i18n) Expert

> **Version:** 2.0.0 | **Updated:** 2026-01-31  
> **next-intl:** v3.x  
> **Priority:** P0 - Load for all i18n projects

---

You are an expert in Next.js internationalization and localization.

## Core i18n Principles

- Use next-intl for App Router i18n
- Implement locale-based routing
- Support multiple languages
- Follow i18n best practices

---

## 1) Complete next-intl Setup

### Installation & Configuration
```bash
npm install next-intl
```

### Project Structure
```
app/
├── [locale]/
│   ├── layout.tsx
│   ├── page.tsx
│   ├── about/
│   │   └── page.tsx
│   └── products/
│       ├── page.tsx
│       └── [id]/
│           └── page.tsx
├── api/
│   └── ...
└── globals.css

messages/
├── en.json
├── es.json
├── fr.json
├── de.json
├── ja.json
├── ar.json
└── vi.json

i18n/
├── config.ts
├── request.ts
└── routing.ts

middleware.ts
next.config.js
```

### Configuration Files
```typescript
// ==========================================
// i18n/config.ts
// ==========================================

export const locales = ['en', 'es', 'fr', 'de', 'ja', 'ar', 'vi'] as const;
export type Locale = (typeof locales)[number];

export const defaultLocale: Locale = 'en';

export const localeNames: Record<Locale, string> = {
  en: 'English',
  es: 'Español',
  fr: 'Français',
  de: 'Deutsch',
  ja: '日本語',
  ar: 'العربية',
  vi: 'Tiếng Việt',
};

export const localeFlags: Record<Locale, string> = {
  en: '🇺🇸',
  es: '🇪🇸',
  fr: '🇫🇷',
  de: '🇩🇪',
  ja: '🇯🇵',
  ar: '🇸🇦',
  vi: '🇻🇳',
};

// RTL languages
export const rtlLocales: Locale[] = ['ar'];

export function isRtlLocale(locale: Locale): boolean {
  return rtlLocales.includes(locale);
}


// ==========================================
// i18n/routing.ts
// ==========================================

import { defineRouting } from 'next-intl/routing';
import { createSharedPathnamesNavigation } from 'next-intl/navigation';
import { locales, defaultLocale } from './config';

export const routing = defineRouting({
  locales,
  defaultLocale,
  localePrefix: 'always',  // or 'as-needed', 'never'
});

// Navigation components with locale handling
export const {
  Link,
  redirect,
  usePathname,
  useRouter,
} = createSharedPathnamesNavigation(routing);


// ==========================================
// i18n/request.ts
// ==========================================

import { getRequestConfig } from 'next-intl/server';
import { routing } from './routing';

export default getRequestConfig(async ({ locale }) => {
  // Validate locale
  if (!routing.locales.includes(locale as any)) {
    return { messages: {} };
  }

  // Load messages for the locale
  const messages = (await import(`../messages/${locale}.json`)).default;

  return {
    messages,
    // Optional: Configure time zone and formats
    timeZone: 'UTC',
    now: new Date(),
    formats: {
      dateTime: {
        short: {
          day: 'numeric',
          month: 'short',
          year: 'numeric',
        },
        long: {
          day: 'numeric',
          month: 'long',
          year: 'numeric',
          weekday: 'long',
        },
      },
      number: {
        currency: {
          style: 'currency',
          currency: 'USD',
        },
        percent: {
          style: 'percent',
          minimumFractionDigits: 2,
        },
      },
    },
  };
});


// ==========================================
// middleware.ts
// ==========================================

import createMiddleware from 'next-intl/middleware';
import { routing } from './i18n/routing';

export default createMiddleware(routing);

export const config = {
  // Match all pathnames except for
  // - /api (API routes)
  // - /_next (Next.js internals)
  // - /static (public static files)
  // - /*.* (files with extensions like /favicon.ico)
  matcher: ['/((?!api|_next|static|.*\\..*).*)'],
};


// ==========================================
// next.config.js
// ==========================================

const createNextIntlPlugin = require('next-intl/plugin');

const withNextIntl = createNextIntlPlugin('./i18n/request.ts');

/** @type {import('next').NextConfig} */
const nextConfig = {
  // your other config
};

module.exports = withNextIntl(nextConfig);
```

---

## 2) Translation Files

### Message Structure
```json
// ==========================================
// messages/en.json
// ==========================================

{
  "common": {
    "loading": "Loading...",
    "error": "An error occurred",
    "retry": "Try again",
    "save": "Save",
    "cancel": "Cancel",
    "delete": "Delete",
    "edit": "Edit",
    "search": "Search",
    "noResults": "No results found"
  },
  
  "navigation": {
    "home": "Home",
    "about": "About",
    "products": "Products",
    "contact": "Contact",
    "blog": "Blog",
    "login": "Log in",
    "signup": "Sign up",
    "logout": "Log out"
  },
  
  "auth": {
    "login": {
      "title": "Welcome back",
      "subtitle": "Sign in to your account",
      "email": "Email address",
      "password": "Password",
      "rememberMe": "Remember me",
      "forgotPassword": "Forgot password?",
      "submit": "Sign in",
      "noAccount": "Don't have an account?",
      "signupLink": "Sign up"
    },
    "errors": {
      "invalidCredentials": "Invalid email or password",
      "emailRequired": "Email is required",
      "passwordRequired": "Password is required"
    }
  },
  
  "home": {
    "hero": {
      "title": "Build amazing products",
      "subtitle": "The platform for developers to create, deploy, and scale",
      "cta": "Get started for free"
    },
    "features": {
      "title": "Why choose us?",
      "fast": {
        "title": "Lightning fast",
        "description": "Our platform is optimized for speed"
      },
      "secure": {
        "title": "Enterprise security",
        "description": "Your data is protected with advanced encryption"
      }
    }
  },
  
  "products": {
    "title": "Our Products",
    "filters": {
      "all": "All",
      "new": "New arrivals",
      "sale": "On sale"
    },
    "card": {
      "addToCart": "Add to cart",
      "outOfStock": "Out of stock",
      "price": "Price: {price, number, currency}"
    },
    "pagination": {
      "showing": "Showing {from} to {to} of {total} products",
      "previous": "Previous",
      "next": "Next"
    }
  },
  
  "footer": {
    "copyright": "© {year} Company. All rights reserved.",
    "terms": "Terms of Service",
    "privacy": "Privacy Policy"
  },
  
  "errors": {
    "notFound": {
      "title": "Page not found",
      "description": "The page you're looking for doesn't exist.",
      "backHome": "Go back home"
    },
    "serverError": {
      "title": "Something went wrong",
      "description": "We're working on fixing this issue.",
      "retry": "Try again"
    }
  },
  
  "notifications": {
    "items": "{count, plural, =0 {No notifications} one {# notification} other {# notifications}}",
    "messages": "{count, plural, =0 {No messages} one {# message} other {# messages}}"
  },
  
  "time": {
    "now": "Just now",
    "ago": "{time} ago",
    "minutes": "{count, plural, one {# minute} other {# minutes}}",
    "hours": "{count, plural, one {# hour} other {# hours}}",
    "days": "{count, plural, one {# day} other {# days}}"
  }
}


// ==========================================
// messages/es.json
// ==========================================

{
  "common": {
    "loading": "Cargando...",
    "error": "Ocurrió un error",
    "retry": "Intentar de nuevo",
    "save": "Guardar",
    "cancel": "Cancelar",
    "delete": "Eliminar",
    "edit": "Editar",
    "search": "Buscar",
    "noResults": "No se encontraron resultados"
  },
  
  "navigation": {
    "home": "Inicio",
    "about": "Acerca de",
    "products": "Productos",
    "contact": "Contacto",
    "blog": "Blog",
    "login": "Iniciar sesión",
    "signup": "Registrarse",
    "logout": "Cerrar sesión"
  },
  
  "auth": {
    "login": {
      "title": "Bienvenido de nuevo",
      "subtitle": "Inicia sesión en tu cuenta",
      "email": "Correo electrónico",
      "password": "Contraseña",
      "rememberMe": "Recuérdame",
      "forgotPassword": "¿Olvidaste tu contraseña?",
      "submit": "Iniciar sesión",
      "noAccount": "¿No tienes una cuenta?",
      "signupLink": "Regístrate"
    }
  },
  
  "notifications": {
    "items": "{count, plural, =0 {Sin notificaciones} one {# notificación} other {# notificaciones}}"
  }
}
```

---

## 3) Layout & Page Components

### Root Layout with Locale
```tsx
// ==========================================
// app/[locale]/layout.tsx
// ==========================================

import { notFound } from 'next/navigation';
import { NextIntlClientProvider } from 'next-intl';
import { getMessages, unstable_setRequestLocale } from 'next-intl/server';
import { locales, isRtlLocale, Locale } from '@/i18n/config';
import { Inter, Noto_Sans_Arabic, Noto_Sans_JP } from 'next/font/google';

const inter = Inter({ subsets: ['latin'], variable: '--font-inter' });
const notoArabic = Noto_Sans_Arabic({ subsets: ['arabic'], variable: '--font-arabic' });
const notoJP = Noto_Sans_JP({ subsets: ['latin'], variable: '--font-jp' });

// Generate static params for all locales
export function generateStaticParams() {
  return locales.map((locale) => ({ locale }));
}

// Dynamic metadata per locale
export async function generateMetadata({
  params: { locale },
}: {
  params: { locale: Locale };
}) {
  // Import messages for metadata
  const messages = (await import(`@/messages/${locale}.json`)).default;
  
  return {
    title: {
      default: 'My App',
      template: '%s | My App',
    },
    description: messages.home?.hero?.subtitle || 'Welcome to My App',
  };
}

export default async function RootLayout({
  children,
  params: { locale },
}: {
  children: React.ReactNode;
  params: { locale: Locale };
}) {
  // Validate locale
  if (!locales.includes(locale)) {
    notFound();
  }

  // Enable static rendering
  unstable_setRequestLocale(locale);

  // Get messages
  const messages = await getMessages();

  // Determine text direction
  const dir = isRtlLocale(locale) ? 'rtl' : 'ltr';

  // Select font based on locale
  const fontClass = locale === 'ar' 
    ? notoArabic.variable 
    : locale === 'ja' 
      ? notoJP.variable 
      : inter.variable;

  return (
    <html lang={locale} dir={dir} className={fontClass}>
      <body>
        <NextIntlClientProvider messages={messages}>
          <Header />
          <main>{children}</main>
          <Footer />
        </NextIntlClientProvider>
      </body>
    </html>
  );
}


// ==========================================
// app/[locale]/page.tsx
// ==========================================

import { useTranslations } from 'next-intl';
import { unstable_setRequestLocale } from 'next-intl/server';
import { Link } from '@/i18n/routing';
import { Locale } from '@/i18n/config';

export default function HomePage({
  params: { locale },
}: {
  params: { locale: Locale };
}) {
  unstable_setRequestLocale(locale);
  const t = useTranslations('home');

  return (
    <div>
      <section className="hero">
        <h1>{t('hero.title')}</h1>
        <p>{t('hero.subtitle')}</p>
        <Link href="/products">{t('hero.cta')}</Link>
      </section>

      <section className="features">
        <h2>{t('features.title')}</h2>
        <div className="grid">
          <div>
            <h3>{t('features.fast.title')}</h3>
            <p>{t('features.fast.description')}</p>
          </div>
          <div>
            <h3>{t('features.secure.title')}</h3>
            <p>{t('features.secure.description')}</p>
          </div>
        </div>
      </section>
    </div>
  );
}
```

---

## 4) Server & Client Components

### Server Component Translations
```tsx
// ==========================================
// SERVER COMPONENT
// ==========================================

import { getTranslations } from 'next-intl/server';

export default async function ProductsPage() {
  const t = await getTranslations('products');
  
  const products = await fetchProducts();

  return (
    <div>
      <h1>{t('title')}</h1>
      
      <div className="filters">
        <button>{t('filters.all')}</button>
        <button>{t('filters.new')}</button>
        <button>{t('filters.sale')}</button>
      </div>

      <div className="grid">
        {products.map(product => (
          <ProductCard key={product.id} product={product} />
        ))}
      </div>
    </div>
  );
}

// With getFormatter for dates/numbers
import { getFormatter } from 'next-intl/server';

export default async function OrderPage() {
  const t = await getTranslations('orders');
  const format = await getFormatter();
  
  const order = await fetchOrder();

  return (
    <div>
      <p>
        {t('orderDate')}: {format.dateTime(order.createdAt, 'long')}
      </p>
      <p>
        {t('total')}: {format.number(order.total, { style: 'currency', currency: 'USD' })}
      </p>
    </div>
  );
}


// ==========================================
// CLIENT COMPONENT
// ==========================================

'use client';

import { useTranslations, useFormatter, useLocale } from 'next-intl';
import { useState } from 'react';

export function ProductCard({ product }: { product: Product }) {
  const t = useTranslations('products.card');
  const format = useFormatter();
  const locale = useLocale();
  const [isAdding, setIsAdding] = useState(false);

  const handleAddToCart = async () => {
    setIsAdding(true);
    await addToCart(product.id);
    setIsAdding(false);
  };

  return (
    <div className="card">
      <img src={product.image} alt={product.name} />
      <h3>{product.name}</h3>
      
      <p className="price">
        {format.number(product.price, { style: 'currency', currency: 'USD' })}
      </p>
      
      <button
        onClick={handleAddToCart}
        disabled={!product.inStock || isAdding}
      >
        {product.inStock ? t('addToCart') : t('outOfStock')}
      </button>
    </div>
  );
}
```

---

## 5) Dynamic Content & Interpolation

### Rich Text & Variables
```tsx
// ==========================================
// INTERPOLATION
// ==========================================

// messages/en.json
{
  "greeting": "Hello, {name}!",
  "itemCount": "You have {count} items",
  "priceTag": "Price: {price, number, currency}",
  "lastUpdated": "Last updated: {date, date, medium}"
}

// Component
function WelcomeMessage({ user }) {
  const t = useTranslations();
  
  return (
    <p>
      {t('greeting', { name: user.name })}
    </p>
  );
}


// ==========================================
// PLURALIZATION
// ==========================================

// messages/en.json
{
  "cartItems": "{count, plural, =0 {Your cart is empty} one {# item in cart} other {# items in cart}}",
  "followers": "{count, plural, =0 {No followers} one {# follower} other {# followers}}"
}

// Component
function CartBadge({ count }) {
  const t = useTranslations();
  
  return <span>{t('cartItems', { count })}</span>;
}


// ==========================================
// RICH TEXT / HTML
// ==========================================

// messages/en.json
{
  "termsAgreement": "By signing up, you agree to our <terms>Terms of Service</terms> and <privacy>Privacy Policy</privacy>.",
  "highlight": "This is <b>important</b> information."
}

// Component
function TermsText() {
  const t = useTranslations();
  
  return (
    <p>
      {t.rich('termsAgreement', {
        terms: (chunks) => <a href="/terms">{chunks}</a>,
        privacy: (chunks) => <a href="/privacy">{chunks}</a>,
      })}
    </p>
  );
}

function ImportantText() {
  const t = useTranslations();
  
  return (
    <p>
      {t.rich('highlight', {
        b: (chunks) => <strong>{chunks}</strong>,
      })}
    </p>
  );
}


// ==========================================
// SELECT (Gender, Categories, etc.)
// ==========================================

// messages/en.json
{
  "notification": "{type, select, message {New message from {sender}} follow {You have a new follower} like {{sender} liked your post} other {New notification}}"
}

// Component
function Notification({ type, sender }) {
  const t = useTranslations();
  
  return <p>{t('notification', { type, sender })}</p>;
}
```

---

## 6) Locale Switcher

### Complete Language Selector
```tsx
// ==========================================
// components/LocaleSwitcher.tsx
// ==========================================

'use client';

import { useLocale, useTranslations } from 'next-intl';
import { usePathname, useRouter } from '@/i18n/routing';
import { locales, localeNames, localeFlags, Locale } from '@/i18n/config';
import { useState, useTransition, useRef, useEffect } from 'react';

export function LocaleSwitcher() {
  const t = useTranslations('common');
  const locale = useLocale() as Locale;
  const router = useRouter();
  const pathname = usePathname();
  const [isPending, startTransition] = useTransition();
  const [isOpen, setIsOpen] = useState(false);
  const dropdownRef = useRef<HTMLDivElement>(null);

  // Close dropdown when clicking outside
  useEffect(() => {
    function handleClickOutside(event: MouseEvent) {
      if (dropdownRef.current && !dropdownRef.current.contains(event.target as Node)) {
        setIsOpen(false);
      }
    }
    
    document.addEventListener('mousedown', handleClickOutside);
    return () => document.removeEventListener('mousedown', handleClickOutside);
  }, []);

  function handleLocaleChange(newLocale: Locale) {
    startTransition(() => {
      router.replace(pathname, { locale: newLocale });
    });
    setIsOpen(false);
  }

  return (
    <div className="relative" ref={dropdownRef}>
      <button
        onClick={() => setIsOpen(!isOpen)}
        className="flex items-center gap-2 px-3 py-2 rounded-lg hover:bg-gray-100"
        disabled={isPending}
        aria-expanded={isOpen}
        aria-haspopup="listbox"
      >
        <span>{localeFlags[locale]}</span>
        <span className="hidden sm:inline">{localeNames[locale]}</span>
        <svg
          className={`w-4 h-4 transition-transform ${isOpen ? 'rotate-180' : ''}`}
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M19 9l-7 7-7-7" />
        </svg>
      </button>

      {isOpen && (
        <ul
          className="absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-lg border py-1 z-50"
          role="listbox"
          aria-label="Select language"
        >
          {locales.map((loc) => (
            <li key={loc}>
              <button
                onClick={() => handleLocaleChange(loc)}
                className={`
                  w-full flex items-center gap-3 px-4 py-2 hover:bg-gray-100
                  ${loc === locale ? 'bg-blue-50 text-blue-600' : ''}
                `}
                role="option"
                aria-selected={loc === locale}
              >
                <span>{localeFlags[loc]}</span>
                <span>{localeNames[loc]}</span>
                {loc === locale && (
                  <svg className="w-4 h-4 ml-auto" fill="currentColor" viewBox="0 0 20 20">
                    <path
                      fillRule="evenodd"
                      d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z"
                      clipRule="evenodd"
                    />
                  </svg>
                )}
              </button>
            </li>
          ))}
        </ul>
      )}

      {isPending && (
        <div className="absolute inset-0 flex items-center justify-center bg-white/50">
          <div className="w-4 h-4 border-2 border-blue-500 border-t-transparent rounded-full animate-spin" />
        </div>
      )}
    </div>
  );
}


// ==========================================
// SIMPLE LOCALE SWITCHER (Links)
// ==========================================

import { Link, usePathname } from '@/i18n/routing';

export function LocaleSwitcherSimple() {
  const pathname = usePathname();
  const locale = useLocale();

  return (
    <div className="flex gap-2">
      {locales.map((loc) => (
        <Link
          key={loc}
          href={pathname}
          locale={loc}
          className={`
            px-2 py-1 rounded text-sm
            ${loc === locale ? 'bg-blue-500 text-white' : 'hover:bg-gray-100'}
          `}
        >
          {loc.toUpperCase()}
        </Link>
      ))}
    </div>
  );
}
```

---

## 7) SEO & Metadata

### Internationalized Metadata
```tsx
// ==========================================
// app/[locale]/layout.tsx - Metadata
// ==========================================

import { Metadata } from 'next';
import { getTranslations } from 'next-intl/server';
import { locales, Locale } from '@/i18n/config';

export async function generateMetadata({
  params: { locale },
}: {
  params: { locale: Locale };
}): Promise<Metadata> {
  const t = await getTranslations({ locale, namespace: 'metadata' });
  
  const baseUrl = process.env.NEXT_PUBLIC_BASE_URL || 'https://myapp.com';

  return {
    title: {
      default: t('title'),
      template: `%s | ${t('siteName')}`,
    },
    description: t('description'),
    
    // Alternate languages
    alternates: {
      canonical: `${baseUrl}/${locale}`,
      languages: Object.fromEntries(
        locales.map((loc) => [loc, `${baseUrl}/${loc}`])
      ),
    },
    
    // Open Graph with locale
    openGraph: {
      title: t('title'),
      description: t('description'),
      url: `${baseUrl}/${locale}`,
      siteName: t('siteName'),
      locale: locale,
      alternateLocale: locales.filter(l => l !== locale),
      type: 'website',
    },
    
    // Twitter
    twitter: {
      card: 'summary_large_image',
      title: t('title'),
      description: t('description'),
    },
  };
}


// ==========================================
// DYNAMIC PAGE METADATA
// ==========================================

// app/[locale]/products/[id]/page.tsx
export async function generateMetadata({
  params: { locale, id },
}: {
  params: { locale: Locale; id: string };
}): Promise<Metadata> {
  const product = await getProduct(id);
  const t = await getTranslations({ locale, namespace: 'products' });
  
  const baseUrl = process.env.NEXT_PUBLIC_BASE_URL;

  return {
    title: product.name,
    description: product.description,
    
    alternates: {
      canonical: `${baseUrl}/${locale}/products/${id}`,
      languages: Object.fromEntries(
        locales.map((loc) => [loc, `${baseUrl}/${loc}/products/${id}`])
      ),
    },
    
    openGraph: {
      title: product.name,
      description: product.description,
      images: [product.image],
      locale: locale,
    },
  };
}


// ==========================================
// SITEMAP WITH LOCALES
// ==========================================

// app/sitemap.ts
import { MetadataRoute } from 'next';
import { locales } from '@/i18n/config';

export default async function sitemap(): Promise<MetadataRoute.Sitemap> {
  const baseUrl = process.env.NEXT_PUBLIC_BASE_URL || 'https://myapp.com';
  
  // Static pages
  const staticPages = ['', '/about', '/contact', '/products'];
  
  // Generate URLs for all locales
  const staticUrls = staticPages.flatMap(page =>
    locales.map(locale => ({
      url: `${baseUrl}/${locale}${page}`,
      lastModified: new Date(),
      changeFrequency: 'weekly' as const,
      priority: page === '' ? 1 : 0.8,
      alternates: {
        languages: Object.fromEntries(
          locales.map(loc => [loc, `${baseUrl}/${loc}${page}`])
        ),
      },
    }))
  );
  
  // Dynamic pages (products)
  const products = await getAllProducts();
  
  const productUrls = products.flatMap(product =>
    locales.map(locale => ({
      url: `${baseUrl}/${locale}/products/${product.slug}`,
      lastModified: product.updatedAt,
      changeFrequency: 'daily' as const,
      priority: 0.6,
      alternates: {
        languages: Object.fromEntries(
          locales.map(loc => [loc, `${baseUrl}/${loc}/products/${product.slug}`])
        ),
      },
    }))
  );
  
  return [...staticUrls, ...productUrls];
}
```

---

## 8) Date & Number Formatting

### Formatter Utilities
```tsx
// ==========================================
// USING useFormatter
// ==========================================

'use client';

import { useFormatter, useLocale, useNow } from 'next-intl';

export function FormattedContent() {
  const format = useFormatter();
  const locale = useLocale();
  const now = useNow();

  const price = 1234.56;
  const count = 1500000;
  const date = new Date('2024-03-15T10:30:00');

  return (
    <div>
      {/* Currency formatting */}
      <p>
        Price: {format.number(price, { style: 'currency', currency: 'USD' })}
        {/* en: $1,234.56 */}
        {/* de: 1.234,56 $ */}
        {/* ja: $1,234.56 */}
      </p>

      {/* Compact number */}
      <p>
        Views: {format.number(count, { notation: 'compact' })}
        {/* en: 1.5M */}
        {/* ja: 150万 */}
      </p>

      {/* Date formatting */}
      <p>
        Date: {format.dateTime(date, 'short')}
        {/* en: Mar 15, 2024 */}
        {/* de: 15. März 2024 */}
        {/* ja: 2024年3月15日 */}
      </p>

      {/* Relative time */}
      <p>
        Posted: {format.relativeTime(date, now)}
        {/* en: 3 days ago */}
        {/* es: hace 3 días */}
      </p>

      {/* List formatting */}
      <p>
        Tags: {format.list(['React', 'Next.js', 'TypeScript'], { type: 'conjunction' })}
        {/* en: React, Next.js, and TypeScript */}
        {/* de: React, Next.js und TypeScript */}
      </p>
    </div>
  );
}


// ==========================================
// RELATIVE TIME COMPONENT
// ==========================================

export function RelativeTime({ date }: { date: Date }) {
  const format = useFormatter();
  const now = useNow({
    updateInterval: 1000 * 60,  // Update every minute
  });

  return <time dateTime={date.toISOString()}>{format.relativeTime(date, now)}</time>;
}
```

---

## 9) RTL Support

### RTL Styles
```css
/* globals.css */

/* Use logical properties for RTL support */
.card {
  margin-inline-start: 1rem;  /* Instead of margin-left */
  margin-inline-end: 1rem;    /* Instead of margin-right */
  padding-inline: 1rem;       /* Both sides */
}

.sidebar {
  inset-inline-start: 0;      /* Instead of left: 0 */
}

.icon-arrow {
  transform: scaleX(1);
}

[dir="rtl"] .icon-arrow {
  transform: scaleX(-1);  /* Flip icons for RTL */
}

/* Text alignment */
.text-start {
  text-align: start;  /* left in LTR, right in RTL */
}

.text-end {
  text-align: end;    /* right in LTR, left in RTL */
}

/* Border radius */
.card {
  border-start-start-radius: 0.5rem;  /* top-left in LTR, top-right in RTL */
  border-start-end-radius: 0.5rem;    /* top-right in LTR, top-left in RTL */
}
```

```tsx
// ==========================================
// RTL-AWARE COMPONENT
// ==========================================

import { useLocale } from 'next-intl';
import { isRtlLocale, Locale } from '@/i18n/config';

export function NavigationArrow({ direction }: { direction: 'next' | 'prev' }) {
  const locale = useLocale() as Locale;
  const isRtl = isRtlLocale(locale);
  
  // Flip arrows for RTL
  const isForward = direction === 'next';
  const showRightArrow = isRtl ? !isForward : isForward;
  
  return (
    <button className="nav-arrow" aria-label={direction}>
      {showRightArrow ? '→' : '←'}
    </button>
  );
}
```

---

## Best Practices Checklist

### Setup
- [ ] next-intl installed
- [ ] Middleware configured
- [ ] Locale routing set up
- [ ] Messages organized by feature

### Components
- [ ] useTranslations in client
- [ ] getTranslations in server
- [ ] Locale switcher working
- [ ] Formatters for dates/numbers

### Content
- [ ] All text translated
- [ ] Pluralization handled
- [ ] Rich text working
- [ ] No hardcoded strings

### SEO
- [ ] hreflang tags
- [ ] Alternate languages
- [ ] Localized sitemap
- [ ] lang attribute set

### RTL
- [ ] Logical CSS properties
- [ ] Direction attribute
- [ ] Icons flipped
- [ ] Layout tested

---

**References:**
- [next-intl Documentation](https://next-intl-docs.vercel.app/)
- [ICU Message Format](https://unicode-org.github.io/icu/userguide/format_parse/messages/)
- [CSS Logical Properties](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Logical_Properties)
